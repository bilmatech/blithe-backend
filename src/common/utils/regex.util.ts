export const passwordRegex =
  /^(?=.*[a-z])(?=.*[A-Z])(?=.*\d)(?=.*[^a-zA-Z0-9]).{8,}$/; // Password must contain at least one uppercase letter, one lowercase letter, one number, and one special character
